import ServicesPage from "@/components/ServicesPage";

export const metadata = {
  title: "Services — COADAL",
  description: "Web development, mobile apps, AI, performance marketing, SEO, brand identity, and automation — everything you need to scale.",
};

export default function Page() {
  return <ServicesPage />;
}
