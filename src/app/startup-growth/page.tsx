import StartupGrowthPage from "@/components/StartupGrowthPage";

export const metadata = {
  title: "Startup Growth — COADAL",
  description: "Startup packages built for founders — from pre-revenue to Series A. Launch, Growth, and Scale plans with clear pricing.",
};

export default function Page() {
  return <StartupGrowthPage />;
}
